// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url      = env("DATABASE_URL")
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
  output   = "node_modules/.prisma/client"
}

model Site {
  id    String @id @default(uuid()) @postgresql.Uuid
  Title String @default("")
  slug  String @default("")
}

model Artwork {
  id                 String       @id @default(uuid()) @postgresql.Uuid
  status             String?      @default("draft")
  title              String       @default("")
  artist             String       @default("")
  relatedExperiences Experience[] @relation("Artwork_relatedExperiences")
  startDate          DateTime?
  endDate            DateTime?
  images             Json?
  artworkImages      String       @default("")
  audioFile          String       @default("")
  description        String       @default("")
  url                String       @unique @default("")
  siteId             String       @default("")
  qrCodes            Json?
}

model User {
  id       String  @id @default(uuid()) @postgresql.Uuid
  siteId   String  @default("")
  isAdmin  Boolean @default(false)
  name     String  @default("")
  email    String  @unique @default("")
  password String
}

model Experience {
  id               String    @id @default(uuid()) @postgresql.Uuid
  siteId           String    @default("")
  title            String    @default("")
  slug             String    @unique @default("")
  poster           Json?
  experienceImages String    @default("")
  startDate        DateTime?
  endDate          DateTime?
  status           String?   @default("draft")
  description      String    @default("")
  relatedArtworks  Artwork[] @relation("Artwork_relatedExperiences")
}

model StaticContent {
  id               String @id @default(uuid()) @postgresql.Uuid
  name             String @default("")
  siteId           String @default("")
  title            String @default("")
  poster           Json?
  staticPageImages String @default("")
  slug             String @unique @default("")
  description      String @default("")
}

model Comment {
  id           String @id @default(uuid()) @postgresql.Uuid
  experienceId String @default("")
  siteId       String @default("")
  artworkId    String @default("")
  username     String @default("")
  image        String @default("")
  comment      String @default("")
}